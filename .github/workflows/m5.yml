name: M5 Max Desktop

on:
  workflow_dispatch:

jobs:
  m5:
    runs-on: avrea-macos-26-8-vcpu
    timeout-minutes: 7200

    steps:
      - name: Keep desktop alive
        run: |
          echo "M5 Max desktop running"
          sleep 432000
